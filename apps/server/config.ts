import { config } from "@repo/common";

export const DEVELOPMENT_URL = config.backend.DEVELOPMENT_URL;
export const PRODUCTION_URL = config.backend.PRODUCTION_URL;
export const PORT = config.backend.PORT;
export const NODE_ENV = config.backend.NODE_ENV;
export const JWT_SECRET = config.backend.JWT_SECRET;
export const RESEND_API_KEY = config.resend.RESEND_API_KEY;
